/// <reference types="request" />
import { CoreOptions, UrlOptions } from 'request';
import { NewsPayload, PhotoPayload } from '../index';
export declare class Utils {
    private constructor();
    static makeRequest(options: UrlOptions & CoreOptions, cb?: Function): Promise<NewsPayload[] | PhotoPayload> | void;
    static getRequestPayload(url: string): UrlOptions & CoreOptions;
    static getLatestNewsUrl(pageSize: number, pageNumber: number): string;
    static getPictureOfDayUrl(startDate: string, numberOfDays: number): string;
}
